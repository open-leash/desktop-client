{
  "rules": [
    {
      "id": "company-secrets",
      "name": "Company secrets",
      "category": "Secrets and credentials",
      "description": "Ask before agents handle company secret markers or internal tokens.",
      "enabled": true,
      "match": ["COMPANY_SECRET", "internal_token", "prod credential"]
    },
    {
      "id": "terraform-production",
      "name": "Production infrastructure",
      "category": "System safety",
      "description": "Ask before agents touch production Terraform state.",
      "enabled": true,
      "pattern": "terraform\\s+(apply|destroy).*(prod|production)"
    }
  ]
}
