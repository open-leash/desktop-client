# Feature Island Contributions

Built-in Features may contribute bounded, typed status to the desktop island. Contributions must use the existing shared contract, identify the source Feature by its stable compatibility ID, and expire automatically.

Feature content cannot inject HTML, arbitrary actions, or executable code. The desktop decides layout and supported actions. Internal field names containing `plugin` remain protocol compatibility names.
