# Feature Outcomes

Every built-in Feature can emit normalized outcomes with a title, summary, severity, decision, session, project, and audit metadata. Desktop, mobile, web, and the flow viewer consume the same records.

Outcomes are product data, not marketplace analytics. Leash does not expose publishers, ratings, install counts, download counts, or community profiles.
