{
  "name": "@openleash/provider-puller",
  "version": "0.1.0",
  "description": "Optional Leash provider activity synchronization scheduler.",
  "repository": {
    "type": "git",
    "url": "https://github.com/open-leash/leash.git",
    "directory": "apps/provider-sync-worker"
  },
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "devDependencies": { "@types/node": "^22.15.21", "typescript": "^5.8.3" }
}
